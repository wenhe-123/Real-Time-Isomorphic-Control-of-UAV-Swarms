# pure_angular

Superellipsoid morph sampling (default backend). Lives under ``functions/pure_angular/``.

- morph_renderers.py
- morph_lp_plot.py
- morph_geometry.py

Alternate backend lives under ``backup/sampling/3region_mapping`` (env ``ISO_SWARM_SAMPLING_BACKEND=3region_mapping``).
