# pure_angular

This folder stores the sampling-point implementation migrated from `src/shared/`.

- morph_renderers.py
- morph_lp_plot.py
- morph_geometry.py

Use together with backend selector in `shared` to switch independently from `3region_mapping`.
