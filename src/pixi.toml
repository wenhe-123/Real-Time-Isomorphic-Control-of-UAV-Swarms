[project]
name = "iso_swarm"
version = "0.1.0"
description = "iso_swarm project"
authors = ["Mia"]
channels = ["conda-forge"]
platforms = ["linux-64"]

[dependencies]
python = ">=3.10,<3.12"

[activation.env]
PYTHONPATH = "src"